export enum State_Enum {
  Disconnected,
  Connected,
  Connecting,
  Disconnecting,
  Unknown,
}

export enum Gender_Enum {
  Male,
  Female,
  Other,
}

export enum Response_Enum {
  New,
  In_Progress,
  Submitted,
  Abandoned,
  Expired,
}